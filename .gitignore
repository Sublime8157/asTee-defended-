/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode

# Database dumps — contain customer PII (names, addresses, contact numbers,
# email addresses, bcrypt password hashes). Never commit these.
*.sql
!database/**/*.sql

# Local tooling / build artefacts that don't belong in version control
composer.phar
*.zip
